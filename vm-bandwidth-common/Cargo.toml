[package]
name = "vm-bandwidth-common"
version.workspace = true
edition.workspace = true

[features]
default = []
user = ["aya"]

[dependencies]
aya = { version = "0.14", optional = true }

[lib]
path = "src/lib.rs"
